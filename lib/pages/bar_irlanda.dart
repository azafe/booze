import 'package:flutter/material.dart';

class IrlandaScreen extends StatelessWidget {
  const IrlandaScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: Text("Bar Irlanda"),
      ),
    );
  }
}
